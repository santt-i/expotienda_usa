{
  "cli": {
    "version": ">= 24.8.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      },
      "installCommand": "npm ci --include=dev --ignore-scripts && npx patch-package"
    },
    "production": {
      "autoIncrement": true,
      "installCommand": "npm ci --include=dev --ignore-scripts && npx patch-package"
    }
  },
  "submit": {
    "production": {}
  }
}